import java.util.Scanner;
import java.util.Random;

public class NumberGuessingGame {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Random rand = new Random();

        int rounds = 3; // total rounds
        int maxAttempts = 7; // limit attempts
        int score = 0; // final score tracker

        System.out.println("🎮 Welcome to the Number Guessing Game!");
        System.out.println("You have " + rounds + " rounds to play.");
        System.out.println("Guess a number between 1 and 100.");
        System.out.println("You get " + maxAttempts + " attempts each round.\n");

        for (int round = 1; round <= rounds; round++) {
            int numberToGuess = rand.nextInt(100) + 1; // random 1-100
            int attempts = 0;
            boolean guessed = false;

            System.out.println("------ Round " + round + " ------");

            while (attempts < maxAttempts) {
                System.out.print("Enter your guess: ");
                int userGuess = sc.nextInt();
                attempts++;

                if (userGuess == numberToGuess) {
                    System.out.println(" Correct! You guessed the number in " + attempts + " attempts.");
                    int roundPoints = (maxAttempts - attempts + 1) * 10; // higher points if fewer attempts
                    score += roundPoints;
                    System.out.println("You earned " + roundPoints + " points this round.");
                    guessed = true;
                    break;
                } else if (userGuess < numberToGuess) {
                    System.out.println("Too low! Try again.");
                } else {
                    System.out.println("Too high! Try again.");
                }
            }

            if (!guessed) {
                System.out.println(" You ran out of attempts! The correct number was: " + numberToGuess);
            }
            System.out.println();
        }

        System.out.println(" Game Over!");
        System.out.println("Your Final Score: " + score);

        sc.close();
    }
}
